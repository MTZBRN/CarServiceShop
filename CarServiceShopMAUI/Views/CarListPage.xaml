<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarServiceShopMAUI.Views.CarListPage"
             Title="Autók"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="IconButton" TargetType="Button">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="WidthRequest" Value="40"/>
                <Setter Property="HeightRequest" Value="40"/>
                <Setter Property="CornerRadius" Value="20"/>
                <Setter Property="Margin" Value="2"/>
            </Style>

            <Style x:Key="CopyButton" TargetType="Button">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="BackgroundColor" Value="#E0E0E0"/>
                <Setter Property="TextColor" Value="#424242"/>
                <Setter Property="Margin" Value="4,0,0,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto" Padding="10">

        <!-- Header -->
        <Frame Grid.Row="0" 
               BackgroundColor="{AppThemeBinding Light=#2196F3, Dark=#1976D2}"
               Padding="15" 
               CornerRadius="12" 
               Margin="0,0,0,10"
               HasShadow="True">
            <Grid ColumnDefinitions="Auto,*">
                <Label Grid.Column="0" 
                       Text="🚗" 
                       FontSize="24" 
                       VerticalOptions="Center"
                       Margin="0,0,10,0"/>
                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                    <Label Text="Autók" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="{Binding Cars.Count, StringFormat='Összesen: {0} autó'}" 
                           FontSize="12"
                           TextColor="White"
                           Opacity="0.9"/>
                </VerticalStackLayout>
            </Grid>
        </Frame>

        <!-- Lista -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Cars}"
                        SelectionMode="None">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" 
                                     VerticalOptions="Center"
                                     Spacing="10">
                    <Label Text="🚗" 
                           FontSize="64" 
                           HorizontalOptions="Center"
                           Opacity="0.3"/>
                    <Label Text="Még nincsenek autók" 
                           FontSize="16"
                           TextColor="Gray"
                           HorizontalOptions="Center"/>
                    <Label Text="Kattints az 'Új autó' gombra a hozzáadáshoz" 
                           FontSize="12"
                           TextColor="Gray"
                           HorizontalOptions="Center"
                           Opacity="0.7"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="16" 
                           Margin="5,5,5,10" 
                           BackgroundColor="{AppThemeBinding Light=White, Dark=#2D2D2D}"
                           CornerRadius="16" 
                           HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto" RowSpacing="8">

                            <!-- Tartalom -->
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Brand}" 
                                       FontAttributes="Bold" 
                                       FontSize="18"
                                       TextColor="{AppThemeBinding Light=#212121, Dark=White}"/>

                                <Label Text="{Binding Model}" 
                                       FontSize="16"
                                       TextColor="{AppThemeBinding Light=#424242, Dark=#E0E0E0}"/>

                                <!-- Rendszám + Copy gomb -->
                                <HorizontalStackLayout Spacing="0" Margin="0,4,0,0">
                                    <Label Text="{Binding LicensePlate}" 
                                           FontSize="14" 
                                           FontAttributes="Bold"
                                           TextColor="{AppThemeBinding Light=#1976D2, Dark=#64B5F6}"
                                           VerticalOptions="Center"/>
                                    <Button Text="📋"
                                            Style="{StaticResource CopyButton}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CopyLicensePlateCommand}"
                                            CommandParameter="{Binding .}"
                                            ToolTipProperties.Text="Rendszám másolása"/>
                                </HorizontalStackLayout>

                                <Label Text="{Binding Year, StringFormat='Évjárat: {0}'}" 
                                       FontSize="13"
                                       TextColor="{AppThemeBinding Light=#616161, Dark=#BDBDBD}"/>
                            </VerticalStackLayout>

                            <!-- Gombok -->
                            <VerticalStackLayout Grid.Column="1" 
                                                 Spacing="8" 
                                                 VerticalOptions="Center"
                                                 Margin="10,0,0,0">
                                <Button Text="🔧"
                                        Style="{StaticResource IconButton}"
                                        BackgroundColor="#FF9800"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewServicesCommand}"
                                        CommandParameter="{Binding .}"
                                        ToolTipProperties.Text="Szervizek"/>

                                <Button Text="✏️"
                                        Style="{StaticResource IconButton}"
                                        BackgroundColor="#2196F3"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditCarCommand}"
                                        CommandParameter="{Binding .}"
                                        ToolTipProperties.Text="Szerkesztés"/>

                                <Button Text="🗑️"
                                        Style="{StaticResource IconButton}"
                                        BackgroundColor="#F44336"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCarCommand}"
                                        CommandParameter="{Binding .}"
                                        ToolTipProperties.Text="Törlés"/>
                            </VerticalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Alsó gombok -->
        <Grid Grid.Row="2" 
              ColumnDefinitions="*,*" 
              ColumnSpacing="10"
              Margin="0,10,0,0">
            <Button Grid.Column="0" 
                    Text="➕ Új autó" 
                    Command="{Binding AddCarCommand}" 
                    BackgroundColor="#4CAF50" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"/>

            <Button Grid.Column="1" 
                    Text="🔄 Frissítés" 
                    Command="{Binding LoadCarsCommand}" 
                    BackgroundColor="#FF9800" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"/>
        </Grid>

    </Grid>

</ContentPage>
