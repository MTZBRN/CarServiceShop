<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarServiceShopMAUI.Views.CarListPage"
             Title="Autók"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="10">

        <!-- Header -->
        <Frame Grid.Row="0" 
               BackgroundColor="{AppThemeBinding Light=#2196F3, Dark=#1976D2}"
               Padding="15" 
               CornerRadius="12" 
               Margin="0,0,0,10"
               HasShadow="True">
            <Grid ColumnDefinitions="Auto,*">
                <Label Grid.Column="0" 
                       Text="🚗" 
                       FontSize="24" 
                       VerticalOptions="Center"
                       Margin="0,0,10,0"/>
                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                    <Label Text="Autók" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="{Binding Cars.Count, StringFormat='Megjelenítve: {0} autó'}" 
                           FontSize="12"
                           TextColor="White"
                           Opacity="0.9"/>
                </VerticalStackLayout>
            </Grid>
        </Frame>

        <!-- ✅ Keresőmező -->
        <Frame Grid.Row="1"
               BackgroundColor="{AppThemeBinding Light=White, Dark=#2D2D2D}"
               CornerRadius="12"
               Padding="10"
               Margin="0,0,0,10"
               HasShadow="True">
            <SearchBar Text="{Binding SearchText}"
                       Placeholder="Keresés (márka, modell, rendszám, tulajdonos)..."
                       PlaceholderColor="Gray"
                       CancelButtonColor="{AppThemeBinding Light=#2196F3, Dark=#64B5F6}"
                       TextColor="{AppThemeBinding Light=Black, Dark=White}"/>
        </Frame>

        <!-- Lista -->
        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Cars}"
                        SelectionMode="None">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" 
                                     VerticalOptions="Center"
                                     Spacing="10">
                    <Label Text="🚗" 
                           FontSize="64" 
                           HorizontalOptions="Center"
                           Opacity="0.3"/>
                    <Label Text="Nincs találat" 
                           FontSize="16"
                           TextColor="Gray"
                           HorizontalOptions="Center"/>
                    <Label Text="Próbálj más keresési kifejezést!" 
                           FontSize="12"
                           TextColor="Gray"
                           HorizontalOptions="Center"
                           Opacity="0.7"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="16" 
                           Margin="5,5,5,10" 
                           BackgroundColor="{AppThemeBinding Light=White, Dark=#2D2D2D}"
                           CornerRadius="16" 
                           HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto" RowSpacing="8">

                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Brand}" 
                                       FontAttributes="Bold" 
                                       FontSize="18"
                                       TextColor="{AppThemeBinding Light=#212121, Dark=White}"/>

                                <Label Text="{Binding Model}" 
                                       FontSize="16"
                                       TextColor="{AppThemeBinding Light=#424242, Dark=#E0E0E0}"/>

                                <HorizontalStackLayout Spacing="0" Margin="0,4,0,0">
                                    <Label Text="{Binding LicensePlate}" 
                                           FontSize="14" 
                                           FontAttributes="Bold"
                                           TextColor="{AppThemeBinding Light=#1976D2, Dark=#64B5F6}"
                                           VerticalOptions="Center"/>
                                    <Button Text="📋"
                                            FontSize="12"
                                            Padding="8,4"
                                            CornerRadius="12"
                                            BackgroundColor="#E0E0E0"
                                            TextColor="#424242"
                                            Margin="4,0,0,0"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.CopyLicensePlateCommand}"
                                            CommandParameter="{Binding .}"/>
                                </HorizontalStackLayout>

                                <Label Text="{Binding YearOfManufacture, StringFormat='Évjárat: {0}'}" 
                                       FontSize="13"
                                       TextColor="{AppThemeBinding Light=#616161, Dark=#BDBDBD}"/>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" 
                                                 Spacing="8" 
                                                 VerticalOptions="Center"
                                                 Margin="10,0,0,0">
                                <Button Text="🔧"
                                        FontSize="16"
                                        Padding="0"
                                        WidthRequest="40"
                                        HeightRequest="40"
                                        CornerRadius="20"
                                        BackgroundColor="#FF9800"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewServicesCommand}"
                                        CommandParameter="{Binding .}"/>

                                <Button Text="✏️"
                                        FontSize="16"
                                        Padding="0"
                                        WidthRequest="40"
                                        HeightRequest="40"
                                        CornerRadius="20"
                                        BackgroundColor="#2196F3"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditCarCommand}"
                                        CommandParameter="{Binding .}"/>

                                <Button Text="🗑️"
                                        FontSize="16"
                                        Padding="0"
                                        WidthRequest="40"
                                        HeightRequest="40"
                                        CornerRadius="20"
                                        BackgroundColor="#F44336"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCarCommand}"
                                        CommandParameter="{Binding .}"/>
                            </VerticalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Alsó gombok -->
        <Grid Grid.Row="3" 
              ColumnDefinitions="*,*" 
              ColumnSpacing="10"
              Margin="0,10,0,0">
            <Button Grid.Column="0" 
                    Text="➕ Új autó" 
                    Command="{Binding AddCarCommand}" 
                    BackgroundColor="#4CAF50" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"/>

            <Button Grid.Column="1" 
                    Text="🔄 Frissítés" 
                    Command="{Binding LoadCarsCommand}" 
                    BackgroundColor="#FF9800" 
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"/>
        </Grid>

    </Grid>

</ContentPage>
