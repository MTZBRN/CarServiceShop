<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarServiceShopMAUI.Views.PartListPage"
             Title="{Binding PageTitle}">
    <Grid RowDefinitions="Auto,*,Auto" Padding="10">
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Parts}"
                        SelectedItem="{Binding SelectedPart}">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="🔩" FontSize="48" HorizontalOptions="Center"/>
                    <Label Text="Nincsenek alkatrészek ehhez a szervizhez" TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="5" BackgroundColor="LightGray" CornerRadius="10">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontAttributes="Bold"/>
                                <Label Text="{Binding PartNumber}" FontSize="12" TextColor="Gray"/>
                                <Label Text="{Binding Quantity, StringFormat='Mennyiség: {0}'}" FontSize="12"/>
                                <Label Text="{Binding Price, StringFormat='Ár: {0:N0} Ft'}" FontSize="12"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="5" VerticalOptions="Center">
                                <Button Text="✏️"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditPartCommand}"
                                        FontSize="12" WidthRequest="50"/>
                                <Button Text="🗑️"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeletePartCommand}"
                                        FontSize="12" WidthRequest="50" BackgroundColor="#F44336" TextColor="White"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,10,0,0">
            <Button Grid.Column="0" Text="➕ Új alkatrész" Command="{Binding AddPartCommand}" BackgroundColor="#4CAF50" TextColor="White"/>
            <Button Grid.Column="1" Text="🔄 Frissítés" Command="{Binding LoadPartsCommand}" BackgroundColor="#FF9800" TextColor="White"/>
        </Grid>
    </Grid>
</ContentPage>