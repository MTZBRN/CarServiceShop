<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="CarServiceShopMAUI.Views.CarDetailPage"
             Title="{Binding PageTitle}"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label Text="Rendszám" FontAttributes="Bold"/>
            <Entry Text="{Binding LicensePlate}" 
                   Placeholder="pl. ABC-123"
                   MaxLength="10"/>

            <Label Text="Márka" FontAttributes="Bold"/>
            <Entry Text="{Binding Brand}" 
                   Placeholder="pl. Toyota"/>

            <Label Text="Modell" FontAttributes="Bold"/>
            <Entry Text="{Binding Model}" 
                   Placeholder="pl. Corolla"/>

            <Label Text="Gyártási év" FontAttributes="Bold"/>
            <Entry Text="{Binding YearOfManufacture}" 
                   Placeholder="pl. 2020"
                   Keyboard="Numeric"/>

            <Label Text="Műszaki vizsga lejárata" FontAttributes="Bold"/>
            <DatePicker Date="{Binding DateOfTechnicalInspection}"
                        MinimumDate="01/01/2020"
                        MaximumDate="12/31/2030"/>

            <Label Text="Tulajdonos neve" FontAttributes="Bold"/>
            <Entry Text="{Binding OwnerName}" 
                   Placeholder="pl. Kovács János"/>

            <Label Text="Tulajdonos címe" FontAttributes="Bold"/>
            <Entry Text="{Binding OwnerAddress}" 
                   Placeholder="pl. Budapest, Kossuth Lajos u. 23."/>

            <Label Text="Tulajdonos telefonszáma" FontAttributes="Bold"/>
            <Entry Text="{Binding OwnerPhone}" 
                   Placeholder="pl. +36 30 123 4567"
                   Keyboard="Telephone"/>

            <Label Text="Alvázszám (VIN)" FontAttributes="Bold"/>
            <Entry Text="{Binding Vin}" 
                   Placeholder="pl. 1HGBH41JXMN109186"
                   MaxLength="17"/>

            <Label Text="Km óra állás" FontAttributes="Bold"/>
            <Entry Text="{Binding Mileage}" 
                   Placeholder="pl. 105000"
                   Keyboard="Numeric"/>

            <Button Text="Mentés" 
                    Command="{Binding SaveCommand}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    Margin="0,20,0,0"/>

            <Button Text="Mégse" 
                    Command="{Binding CancelCommand}"
                    BackgroundColor="#F44336"
                    TextColor="White"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
