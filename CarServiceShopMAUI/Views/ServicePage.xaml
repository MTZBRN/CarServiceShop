<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarServiceShopMAUI.Views.ServicePage"
             Title="{Binding PageTitle}"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="SmallIconButton" TargetType="Button">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="WidthRequest" Value="36"/>
                <Setter Property="HeightRequest" Value="36"/>
                <Setter Property="CornerRadius" Value="18"/>
                <Setter Property="Margin" Value="2"/>
            </Style>
            <Style x:Key="MainButton" TargetType="Button">
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="CornerRadius" Value="22"/>
                <Setter Property="HeightRequest" Value="44"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Margin" Value="0,8,0,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto" Padding="10">

        <!-- Fejléc autó info -->
        <Frame Grid.Row="0" 
               BackgroundColor="{AppThemeBinding Light=#2196F3, Dark=#1976D2}"
               Padding="15"
               CornerRadius="12"
               Margin="0,0,0,10"
               HasShadow="True">
            <Label Text="{Binding CarInfo}" 
                   FontSize="18" 
                   FontAttributes="Bold"
                   TextColor="White"/>
        </Frame>

        <!-- Lista -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Services}"
                        SelectionMode="None">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10">
                    <Label Text="🔧" FontSize="56" Opacity="0.25"/>
                    <Label Text="Még nincs szerviz ehhez az autóhoz" FontSize="15" TextColor="Gray"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="15" Margin="4,6" BackgroundColor="White" CornerRadius="14" HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto" RowSpacing="7">
                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                <Label Text="{Binding ServiceDate, StringFormat='📅 {0:yyyy.MM.dd}'}" FontSize="13" TextColor="#757575"/>
                                <Label Text="{Binding ServiceDescription}" FontSize="16" FontAttributes="Bold"/>
                                <HorizontalStackLayout Spacing="13" Margin="0,2,0,0">
                                    <Label Text="{Binding WorkHours, StringFormat='⏱️ {0} óra'}" FontSize="13" TextColor="#616161"/>
                                    <Label Text="{Binding TotalPrice, StringFormat='💰 {0:N0} Ft'}" FontSize="14" FontAttributes="Bold" TextColor="#2E7D32"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            <!-- Sor gombok -->
                            <VerticalStackLayout Grid.Column="1" Spacing="7" VerticalOptions="Center">
                                <Button Text="🔩 Alkatrészek"
                                        Style="{StaticResource SmallIconButton}"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewPartsCommand}"
                                        CommandParameter="{Binding .}"/>
                                <Button Text="✏️"
                                        Style="{StaticResource SmallIconButton}"
                                        BackgroundColor="#2196F3"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditServiceCommand}"
                                        CommandParameter="{Binding .}"/>
                                <Button Text="🗑️"
                                        Style="{StaticResource SmallIconButton}"
                                        BackgroundColor="#F44336"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteServiceCommand}"
                                        CommandParameter="{Binding .}"/>
                                <Button Text="📝 Worksheet"
                                        Style="{StaticResource SmallIconButton}"
                                        BackgroundColor="#FF9800"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ExportWorksheetCommand}"
                                        CommandParameter="{Binding .}"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Bottom gomb -->
        <Grid Grid.Row="2" ColumnDefinitions="*,*,*" Margin="0,10,0,0" ColumnSpacing="8">
            <Button Grid.Column="0"
                    Text="➕ Új szerviz"
                    Command="{Binding AddServiceCommand}"
                    Style="{StaticResource MainButton}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"/>
            <Button Grid.Column="1"
                    Text="🔄 Frissítés"
                    Command="{Binding LoadServicesCommand}"
                    Style="{StaticResource MainButton}"
                    BackgroundColor="#FF9800"
                    TextColor="White"/>
            <Button Grid.Column="2"
                    Text="⬅️ Vissza"
                    Command="{Binding BackCommand}"
                    Style="{StaticResource MainButton}"
                    BackgroundColor="#9E9E9E"
                    TextColor="White"/>
        </Grid>
    </Grid>
</ContentPage>
