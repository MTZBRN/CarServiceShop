<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CarServiceShopMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CarServiceShopMAUI"
    xmlns:views="clr-namespace:CarServiceShopMAUI.Views"
    Title="CarServiceShopMAUI">
    
    
    <!--<FlyoutItem>
        <ShellContent
            Title="MainPage"
            ContentTemplate="{DataTemplate local:MainPage}" 
            Route="MainPage"/>
    </FlyoutItem>
    <FlyoutItem>
        <ShellContent
            Title="CarListPage"
            ContentTemplate="{DataTemplate views:CarListPage}" 
            Route="CarListPage"/>
    </FlyoutItem>
    <FlyoutItem>
        <ShellContent
            Title="MainPage"
            ContentTemplate="{DataTemplate views:InvoicePage}" 
            Route="InvoicePage"/>
    </FlyoutItem>-->
</Shell>
